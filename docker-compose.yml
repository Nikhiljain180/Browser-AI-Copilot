version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PROXY_PORT=3000
      - PROXY_HOST=0.0.0.0
    restart: unless-stopped
